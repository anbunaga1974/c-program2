#include<stdio.h>
int main()
{
        int num;
        printf("Enter a number:");
        scanf("%d",&num);
        if (num%2==0)
                printf("It is even number");
        else
                printf("It is odd number");
        printf("\nEnd");
        return 0;
}
